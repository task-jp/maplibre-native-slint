import { Button, VerticalBox, ComboBox, HorizontalBox, Slider } from "std-widgets.slint";

// Define a struct to hold window size
export struct Size {
    width: length,
    height: length,
}

// Define enum for POI marker types
export enum MarkerType {
    icon,    // Use custom icon image
    circle,  // Use colored circle
    text,    // Use text label
}

// Define enum for pitch alignment
export enum PitchAlignment {
    map,      // Affected by pitch (rotates with map)
    viewport, // Not affected by pitch (always parallel to screen)
}

// Define a struct to hold POI (Point of Interest) data
export struct POI {
    name: string,
    latitude: float,
    longitude: float,
    marker-type: MarkerType,
    icon: string,
    pitch-alignment: PitchAlignment,
}

export global MapAdapter {
    in-out property <image> map_texture;

    callback tick_map_loop();
    callback mouse_press(float, float);
    callback mouse_release(float, float);
    callback mouse_move(float, float, bool);
    callback style_changed(string);
    callback double_click_with_shift(float, float, bool);
    callback wheel_zoom(float, float, float);
    callback fly_to(string);
    callback pitch_changed(int);
    callback bearing_changed(float);
    callback pois_updated([POI]);
    callback register_poi_icon(string, image);
}

export component MapWindow inherits Window {
    // Default window size
    preferred-width: 800px;
    preferred-height: 600px;

    // Expose window size via Size-typed property
    out property<Size> window-size: { width: root.width, height: root.height };

    // Callback when the window size changes
    callback window-size-changed;

    // Invoke callback when window-size changes
    changed window-size => { window-size-changed(); }

    // Expose map image area size so backend can match framebuffer size
    out property<Size> map-size: { width: map.width, height: map.height };
    callback map-size-changed;
    changed map-size => { map-size-changed(); }

    property <[string]> style_urls: [
        "https://demotiles.maplibre.org/style.json",
        "https://tile.openstreetmap.jp/styles/osm-bright/style.json"
    ];

    // POI data - dummy examples
    in-out property <[POI]> pois: [
        { name: "Tokyo Tower", latitude: 35.6586, longitude: 139.7454, marker-type: MarkerType.icon, icon: "landmark.png", pitch-alignment: PitchAlignment.viewport },
        { name: "Shibuya Station", latitude: 35.6580, longitude: 139.7016, marker-type: MarkerType.circle, pitch-alignment: PitchAlignment.map },
        { name: "Senso-ji Temple", latitude: 35.7148, longitude: 139.7967, marker-type: MarkerType.text, pitch-alignment: PitchAlignment.viewport },
        { name: "Eiffel Tower", latitude: 48.8584, longitude: 2.2945, marker-type: MarkerType.icon, icon: "landmark.png", pitch-alignment: PitchAlignment.map },
        { name: "Statue of Liberty", latitude: 40.6892, longitude: -74.0445, marker-type: MarkerType.circle, pitch-alignment: PitchAlignment.viewport },
    ];

    // Notify backend when POIs change
    changed pois => {
        MapAdapter.pois_updated(root.pois);
    }

    Timer {
        interval: 16ms;
        running: true;
        triggered => {
            MapAdapter.tick_map_loop();
        }
    }

    VerticalBox {
        HorizontalBox {
            ComboBox {
                model: root.style_urls;
                selected(url) => {
                    MapAdapter.style_changed(url);
                }
            }
            Button {
                text: "Paris";
                clicked => {
                    MapAdapter.fly_to("paris");
                }
            }
            Button {
                text: "New York";
                clicked => {
                    MapAdapter.fly_to("new_york");
                }
            }
            Button {
                text: "Tokyo";
                clicked => {
                    MapAdapter.fly_to("tokyo");
                }
            }
            Text {
                text: "Pitch:";
                vertical-alignment: center;
            }
            Slider {
                minimum: 0;
                maximum: 100;
                value: 0;
                changed value => {
                    MapAdapter.pitch_changed(self.value);
                }
            }
            Text {
                text: "Bearing:";
                vertical-alignment: center;
            }
            Slider {
                minimum: 0;
                maximum: 100;
                value: 0;
                changed value => {
                    MapAdapter.bearing_changed(self.value);
                }
            }
        }
        map := Image {
            source: MapAdapter.map_texture;

            touch := TouchArea {
                // Track last seen Shift modifier state from pointer events
                property <bool> last-shift: false;
                mouse-cursor: self.pressed ? grabbing : grab;

                // Handle all pointer events properly
                pointer-event(e) => {
                    self.last-shift = e.modifiers.shift;

                    if (e.kind == PointerEventKind.down) {
                        MapAdapter.mouse_press(self.mouse-x / 1px, self.mouse-y / 1px);
                    } else if (e.kind == PointerEventKind.up) {
                        MapAdapter.mouse_release(self.mouse-x / 1px, self.mouse-y / 1px);
                    } else if (e.kind == PointerEventKind.move && self.pressed) {
                        MapAdapter.mouse_move(self.mouse-x / 1px, self.mouse-y / 1px, true);
                    }
                }

                // Double-click to zoom in (backend handles clamping)
                double-clicked => {
                    MapAdapter.double_click_with_shift(self.mouse-x / 1px,
                                                       self.mouse-y / 1px,
                                                       self.last-shift);
                }

                // Mouse-wheel / trackpad scroll to zoom
                scroll-event(event) => {
                    // Positive delta_y: zoom out; Negative: zoom in
                    MapAdapter.wheel_zoom(self.mouse-x / 1px,
                                           self.mouse-y / 1px,
                                           event.delta_y / 1px);
                    return accept;
                }
            }
        }
    }
}